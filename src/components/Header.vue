<template>
  <header>
    <h1>{{ title }}</h1>
    <Button
      v-if="homePage"
      :text="showTask ? 'Close' : 'AddTask'"
      :color="showTask ? 'orangered' : 'rgb(16, 240, 16)'"
      @btn-click="$emit('toggle-add-task')"
    />
  </header>
</template>

<script>
import Button from "./Button";
export default {
  name: "Header",
  props: {
    title: String,
    showTask: Boolean,
  },
  components: {
    Button,
  },
  computed: {
    homePage() {
      if (this.$route.path === "/") {
        return true;
      } else {
        return false;
      }
    },
  },
};
</script>

<style scoped>
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30px;
}
</style>
